package com.google.appinventor.client.explorer.project;

/**
 * This interface abstracts the Gallery methods.
 *
 * @author wolberd@google.com (David Wolber)
 */
public interface Gallery {
 /* 
	void Publish(GalleryApp app);
	void Update(GalleryApp app);
	
	List<GalleryApp> GetApps(start,count,sortOrder, sortField);
	
	List<GalleryApp> FindApps(start,count,keywords,sortOrder);

	List<GalleryApp> FindByTag(start,count,tag,sortOrder); //   we may not have tags
	
	List<GalleryApp> GetFeatured(start,count,sortOrder);
	
	List<GalleryApp> GetMostDownloaded(start,count);
	
	List<GalleryApp> GetMostViewed(start,count);
	
	List<GalleryApp> GetAppsByDeveloper(start,count,user);
	
	GalleryApp GetProject();
	
	List <String> GetCategories()  // we may not have this
	
*/
	
}
