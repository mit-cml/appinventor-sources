@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix ai: <http://dig.csail.mit.edu/2014/PrivacyInformer/appinventor#> .
@prefix android: <http://dig.csail.mit.edu/2014/PrivacyInformer/android#> .
@prefix location: <http://dig.csail.mit.edu/2014/PrivacyInformer/LocationSensor#> .

# LocationSensor is a component
# It is also a sensor
# It is used to gather data
# It does not require User, but android permission access
# Implemented using android.hardware.SensorListener
# It measure latitude, longitude and altitude (if supported) 

# Component Description
location:LocationSensorComponent  
    a    ai:Component;
    ai:category     ai:Sensor;
    ai:description  "senses the Android device's location with latitude and longitude values, can also convert physical addresses to lat/long values, as well as detect when the location changes";
    ai:details      "altitude can also be measured when the device supports it",
                    "implemented using android.hardware.SensorListener";

# Privacy Implication and Common Usage
    ai:access       android:CoarseLocation, android:FineLocation;
    ai:example      "Google Maps", "TrackMe";
    ai:dataUsage    ai:DetectLocation;
    ai:purpose      ai:DataGathering; 
    ai:requires     android:CoarseLocation, android:FineLocation;

# Component Properties, Methods and Events 
    ai:hasProperty  location:Latitude, location:Longitude, location:Altitude;
    ai:hasMethod    location:LatitudeFromAddress, location:LongitudeFromAddress;
    ai:hasEvent     location:LocationChanged .

# Instance Definitions
location:Latitude   a   ai:ComponentProperty; 
                    rdfs:label  "device's location by latitude " .
location:Longitude  a   ai:ComponentProperty;
                    rdfs:label  "device's location by longitude " .
location:Altitude   a   ai:ComponentProperty;
                    rdfs:label  "device's location by altitude " .

location:LatitudeFromAddress    a   ai:ComponentMethod;
                                rdfs:label  "from a physical address, get the corresponding latitude value " .

location:LongitudeFromAddress   a   ai:ComponentMethod;
                                rdfs:label  "from a physical address, get the corresponding longitude value " .

location:LocationChanged   a   ai:ComponentEvent;
                           rdfs:label  "location sensor's event that detects when its value changes " .
