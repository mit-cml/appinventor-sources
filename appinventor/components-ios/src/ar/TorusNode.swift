// -*- mode: swift; swift-mode:basic-offset: 2; -*-
// Copyright © 2019 Massachusetts Institute of Technology, All rights reserved.

import Foundation
import SceneKit

@available(iOS 14.0, *)
open class TorusNode { //}: ARNodeBase, ARTorus {
  /*private var _torusGeometry: SCNTorus = SCNTorus(ringRadius: 0.04, pipeRadius: 0.01)
  private var _torusNode: SCNNode
  
  @objc init(_ container: ARNodeContainer) {
    _torusNode = SCNNode(geometry: _torusGeometry)
    super.init(container: container, node: _torusNode)
  }
  
  required public init?(coder aDecoder: NSCoder) {
    fatalError("init(coder:) has not been implemented")
  }
  
  @objc open var RingRadiusInCentimeters: Float {
    get {
      return UnitHelper.metersToCentimeters(_torusGeometry.ringRadius)
    }
    set(radius) {
      _torusGeometry.ringRadius = UnitHelper.centimetersToMeters(abs(radius))
    }
  }
  
  @objc open var PipeRadiusInCentimeters: Float {
    get {
      return UnitHelper.metersToCentimeters(_torusGeometry.pipeRadius)
    }
    set(radius) {
      _torusGeometry.pipeRadius = UnitHelper.centimetersToMeters(abs(radius))
    }
  }
   */
}
