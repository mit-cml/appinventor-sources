- release_tracks: [ALPHA]

  help_text:
    brief: Delete an Edge Container.
    description: Delete an Edge Container.

  request:
    collection: edge.projects.locations.registries.devices.containers

  arguments:
    resource:
      help_text: The Edge Container to delete.
      spec: !REF googlecloudsdk.command_lib.iot.edge.resources:container
