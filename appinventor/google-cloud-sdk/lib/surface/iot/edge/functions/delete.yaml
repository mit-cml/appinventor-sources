- release_tracks: [ALPHA]

  help_text:
    brief: Delete an Edge Function.
    description: Delete an Edge Function.

  request:
    collection: edge.projects.locations.registries.devices.functions

  arguments:
    resource:
      help_text: The Edge Function to delete.
      spec: !REF googlecloudsdk.command_lib.iot.edge.resources:function
