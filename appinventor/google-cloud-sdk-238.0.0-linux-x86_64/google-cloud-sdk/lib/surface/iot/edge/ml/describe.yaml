- release_tracks: [ALPHA]

  help_text:
    brief: Display details of an Edge ML Model.
    description: Display details of an Edge ML Model.

  request:
    collection: edge.projects.locations.registries.devices.mlModels

  arguments:
    resource:
      help_text: The Edge ML Model to describe.
      spec: !REF googlecloudsdk.command_lib.iot.edge.resources:mlModel
