/* eslint-env mocha */
'use strict'

describe('FirefoxLauncher', function () {
  it('works', function () {
    const result = 1 + 1

    if (result !== 2) {
      throw new Error('fail')
    }
  })
})
